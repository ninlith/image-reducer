services:
  kollikissa:
    build: .
    ports:
      - "8000:8000"
    environment:
      - VIRTUAL_ENV=/app/.venv
      - PATH=/app/.venv/bin:$PATH
    command: uvicorn src.kollikissa.main:app --host 0.0.0.0 --port 8000
